# Fastfetch

System information tool.

## Installation

```bash
brew install fastfetch
```

## Configuration

Config: `~/.config/fastfetch/` (symlinked via stow)

## Usage

```bash
fastfetch
```

Add to `~/.zshrc` to run on terminal launch.
