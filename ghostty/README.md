# Ghostty

GPU-accelerated terminal emulator.

## Installation

```bash
brew install --cask ghostty
```

## Configuration

Config: `~/.config/ghostty/config` (symlinked via stow)

Launch Ghostty and configuration will be loaded automatically.
